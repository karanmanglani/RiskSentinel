# Setup

## To run backend

-- Activate Virtual Environment
-- go to backend directory and use this command "uvicorn app.main:app --reload"
-- for frontend go to its directory and run command "npm run dev"
